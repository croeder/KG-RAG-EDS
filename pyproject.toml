[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]         # pycodestyle errors, pyflakes, import sorting
ignore = ["E501"]                # ruff format handles code width; don't nag long comments/strings
